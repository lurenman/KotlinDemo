<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.kotlindemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.BasicGrammarActivity" />
        <activity android:name=".activity.BasicDataTypeActivity" />
        <activity android:name=".activity.ControlActivity" />
        <activity android:name=".activity.ClassAndObjectActivity" />
        <activity android:name=".activity.ExtendsAndInterfaceActivity" />
        <activity android:name=".activity.ExtensionActivity" />
        <activity android:name=".activity.DataClassAndSealedClassActivity" />
        <activity android:name=".activity.GenericActivity" />
        <activity android:name=".activity.EnumActivity" />
    </application>

</manifest>